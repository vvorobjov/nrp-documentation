# Documentation build requirements (EBR2-49 — Sphinx 7.x + MyST).
#
# Toolchain bumped from Sphinx 3.4.3 / recommonmark 0.7.1 / Jinja2<3.1
# (all EOL) to Sphinx 7.x with MyST-Parser as the Markdown extension.
# The Jinja2 cap is removed — Sphinx 7 needs Jinja2 >= 3.1.
sphinx>=7.0,<8.0
sphinx-rtd-theme>=2.0
sphinxcontrib-apidoc>=0.4
sphinxcontrib-httpdomain>=1.8
myst-parser>=2.0
sphinx-copybutton>=0.5
sphinxcontrib-images>=0.9.4
docutils>=0.18,<0.21
